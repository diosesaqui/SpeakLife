{
  "name": "speaklife-functions",
  "description": "SpeakLife Firebase Cloud Functions",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "test:rules": "cd .. && firebase emulators:exec --only firestore --project speaklife-rules-test \"node functions/test/rules.test.js\"",
    "test:functions": "cd .. && firebase emulators:exec --only firestore --project speaklife-fn-test \"node functions/test/standTogether.test.js\"",
    "test": "npm run test:rules && npm run test:functions"
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  },
  "private": true,
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
